name: Build Parion APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout v4.0.56 source
        uses: actions/checkout@v4
        with:
          ref: parion-v4-v62-overlay
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'
      - name: Restore permanent Parion signing key
        env:
          PARION_KEYSTORE_BASE64: ${{ secrets.PARION_KEYSTORE_BASE64 }}
        run: |
          test -n "$PARION_KEYSTORE_BASE64"
          printf '%s' "$PARION_KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/parion-release.jks"
      - name: Build signed release APK
        env:
          PARION_KEYSTORE_PATH: ${{ runner.temp }}/parion-release.jks
          PARION_STORE_PASSWORD: ${{ secrets.PARION_STORE_PASSWORD }}
          PARION_KEY_PASSWORD: ${{ secrets.PARION_KEY_PASSWORD }}
          PARION_KEY_ALIAS: ${{ secrets.PARION_KEY_ALIAS }}
        run: gradle assembleRelease --stacktrace
      - name: Verify APK signature
        run: "$ANDROID_HOME/build-tools/36.0.0/apksigner" verify --verbose --print-certs app/build/outputs/apk/release/app-release.apk
      - name: Verify v4.0.56 package
        run: |
          unzip -l app/build/outputs/apk/release/app-release.apk | grep -q classes.dex
          grep -q 'MainActivityV656' app/src/main/AndroidManifest.xml
          grep -q 'MainActivityV656.class' app/src/main/java/com/parion/aidat/SplashActivity.java
          grep -q 'versionName = "4.0.56"' app/build.gradle.kts
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV640.java
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV641.java
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV642.java
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV643.java
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV649.java
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV650.java
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV651.java
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV652.java
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV653.java
          ! grep -q 'postDelayed' app/src/main/java/com/parion/aidat/MainActivityV654.java
      - name: Rename APK
        run: cp app/build/outputs/apk/release/app-release.apk Parion_V4_0_56_No_Legacy_Dashboard_Delays.apk
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: Parion-v4.0.56-No-Legacy-Dashboard-Delays
          path: Parion_V4_0_56_No_Legacy_Dashboard_Delays.apk
          if-no-files-found: error
